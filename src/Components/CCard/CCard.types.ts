import React from 'react';

export interface Props {
    children: React.ReactNode;
    borderRadius?: number;
    borderWidth?: number;
    elevation?: number;
    loading?: boolean;
    style?: any;
}
